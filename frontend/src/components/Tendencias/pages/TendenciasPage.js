import React from 'react';
import { LayoutCMS } from '../../common';

export const TendenciasPage = (props) => {
  return (
    <LayoutCMS>
      <h1>
        Tendencias
      </h1>
    </LayoutCMS>
  );
}

